# 公众号 DeepSeek 手工交接设计

用户主动要求热点深评或文学、典籍稿时，Agent 只研究并提供完整 DeepSeek 提示词，不直接调用或控制 DeepSeek。用户自行与 DeepSeek 交流并把输出贴回后，Agent 再核实、编辑、配图和推送。

流程为：Agent 展示完整提示词并等待第一次确认；用户自行取稿并贴回；Agent 核实编辑和配真实图片；用户第二次确认后才写入微信公众号草稿箱。未收到用户贴回的输出时暂停，不调用 DeepSeek，也不改用其他模型代写。

本规则不影响定时热点与非公众号项目的模型选择。热点图片继续抖音搜索卡片封面优先，禁止自动生成图片。
