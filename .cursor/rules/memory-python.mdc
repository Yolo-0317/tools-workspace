---
description: stock-ai 子项目记忆 — Python / 数据 / 选股
alwaysApply: false
---

# Memory: stock-ai (Python)

按 `project-memory.mdc` 的主题索引精确检索；不要因处理 `stock-ai/` 就整份加载。

## 关键路径

- 共享库：`stock_ai/`（bootstrap、logging、notify、symbols）
- 策略核心：`core_v2/`、`core_v3/`
- 脚本：`scripts/sync|selection|analysis|monitor|tools/`
- MCP：`tushare_mcp.py`；Cursor MCP 配置见 `~/.cursor/mcp.json`

## 记忆条目

### 2026-05-29 脚本 import 规则

- **Context**: 从 `scripts/` 子目录运行入库、选股脚本。
- **Pitfall/Error**: `from _bootstrap import` 在 `scripts/sync/` 下找不到模块。
- **Solution/Rule**: 使用 `from scripts._bootstrap import ensure_repo_root_on_path`，再 `ensure_repo_root_on_path()`。OpenCLI SOP 数据在 `core_v2.fetch_opencli_sop`（`fetch_akshare_data` 仅为兼容别名）。

### 2026-05-30 弃用脚本删除与数据分层

- **Context**: 用户确认「已弃用的就删除」，在 DB + OpenCLI 主链路落地后清理遗留入口。
- **Pitfall/Error**: 东财 `push2`/HTTP 入库、Playwright 基本面、AkShare 校验、CSV 持仓分析等脚本与文档并存，Agent 或 cron 可能误跑旧路径；`stock_ai/http_direct.py` 仅被已删 sync 脚本引用。
- **Solution/Rule**:
  - **已删**：`scripts/sync/ingest_eastmoney_*`、`poll_eastmoney_*`；`core_v2/sync_capital_flow.py`、`get_fundamental_playwright.py`；`scripts/analysis/{analyze_holdings,fetch_fundamentals_only,ai_review_top5,run_deepseek_*}.py`；`scripts/tools/{verify_data,compare_daily_selection}.py`；`stock_ai/http_direct.py`；Playwright/AkShare 手动测试。
  - **保留主入口**：日线 `sync/sync_tushare_daily_to_mysql.py`；OpenCLI `tools/fetch_eastmoney_quotes.py`；选股写/读 `selection_daily_results`（`portfolio_db` + `selection_results.py`）；持仓 `portfolio_positions`；审查 `ai_review_combined_top5.py`、`sop_review_top5_concurrent.py`；持仓 DeepSeek `analyze_holdings_v2.py`（仅 DB）。
  - **文档**：`README.md`、`CAPABILITIES.md`、`docs/pipelines/daily_stock_deepseek_pipeline.md`、`investment-agent/docs/skills/stock-strategy-selector/` 与 `~/.codex/skills/stock-strategy-selector/` 已同步；勿再文档化东财 HTTP 入库方案。

### 2026-05-30 选股入库 strategy 分桶 + 三项收尾

- **Context**: 五因子入库、`REPORT_ONLY` 查 MySQL、重命名 `fetch_akshare_data` → `fetch_opencli_sop`。
- **Pitfall/Error**: 原 `save_selection_daily_results` 按 `trade_date` 整表 DELETE，五因子与综合选股同日会互相覆盖；`REPORT_ONLY=1` 只查 CSV。
- **Solution/Rule**: 表加 `strategy` 列（迁移 `stock-mysql/sql/004_selection_results_strategy.sql`）；写入规则 = **同日 + 同 strategy 先删后插**（覆盖），不同 strategy/日期并存。读默认 `strategy=combined`；五因子读/写 `five_factor`（`deepseek_analyze_five_factor.py` 已 MySQL 优先）。模块：`core_v2/fetch_opencli_sop.py`（`fetch_akshare_data.py` 仅兼容别名）。

### 2026-05-30 数据分层四档（DB / OpenCLI / 文件 / 旁路）

- **Context**: 整体审计后用户追问「设计如此（不进 DB / 不用 OpenCLI）」指什么，需持久化以免 Agent 误判为遗漏。
- **Pitfall/Error**: 把 Markdown 执行卡、MCP Tushare 直查、LLM 层、微信推送、五因子 CSV、output 报告当成「应接 DB/OpenCLI 但未接」；或把 OpenCLI 实时行情当成「应入库」。
- **Solution/Rule**: 四档对照表见 `stock-ai/docs/CAPABILITIES.md` §1「数据分层四档」。**① 主链路**=生产自动化；**②**=OpenCLI 不落库；**③**=DB 主源 + 文件备份/桥接；**④**=纪律 md、Tushare ad-hoc、DeepSeek、wechat-acp、研究脚本与 output 交付物。

### 2026-05-31 看板每日快照 + SOP 落库

- **Context**: 用户要做看板，需保留每日持仓/SOP 历史而非仅当前态。
- **Pitfall/Error**: `portfolio_positions` 只有最新；`sop_review_latest.json` 单文件覆盖。
- **Solution/Rule**: 迁移 `005_dashboard_snapshots.sql` → `portfolio_account_daily`、`portfolio_positions_daily`、`sop_review_daily`、`sop_review_items`。快照 slot：`midday`（15:00 战报后）、`eod`（17:30）、`sync`（改卡）。失败写 `logs/snapshot_alerts.log`，不阻断主流程。遗留表 `006_drop_legacy_eastmoney_tables.sql` 删除 capital_flow / intraday / orderbook。

### 2026-06-02 情绪周期 + 龙头观察池（游资轨数据层）

- **Context**: 龙头战法数据权威在 MySQL，从 `stock_daily` 自动计算；供 home-hub `/emotion`、Agent 题材建议、emquant `dragons.json` 导出。
- **Pitfall/Error**: ① `portfolio_db.mysql_url()` / `emotion_cycle_compute.get_engine()` 在 Docker 内勿把 `host.docker.internal` 改 `127.0.0.1`（用 `/.dockerenv` 判断）。② `latest_emotion_trade_date` 缺失会导致 home-hub API 503。③ 龙头 `name` 若与代码相同，检查入库时 `save_emotion_cycle_checklist` 的 name 字段。
- **Solution/Rule**:
  - **表**：`emotion_cycle_daily`（`trade_date`+`checklist_slot`）、`emotion_cycle_dragon_watch`（Top3，`rank_no`）；SQL `stock-mysql/sql/010_emotion_cycle.sql`。
  - **核心**：`stock_ai/emotion_cycle_compute.py` — `infer_phase`（冰点/启动/发酵/高潮/分歧/退潮）、`_score_dragon`（7 项简化为 pass 分，≥5 入池）、`sync_emotion_cycle`。
  - **CLI**：`scripts/tools/emotion_cycle_checklist.py`（template/save/show/list）；`scripts/tools/sync_emotion_cycle_daily.py`；shell `./sync_emotion_cycle.sh`。
  - **DB API**：`portfolio_db.save/load_emotion_cycle_checklist`、`list_emotion_cycle_trade_dates`、`latest_emotion_trade_date`。
  - **调度**：`docker/scheduler/crontab` — 17:30 Tushare、17:45 选股、**18:00** `eod`（无盘前 `pre_market`）；日志 `emotion_cycle.log`。
  - **文档纪律**：`investment-agent/情绪周期日检卡.md`（与执行卡并行，冲突以执行卡为准）。

### 2026-06-02 个股分析现价：OpenCLI 实时 vs Tushare 昨收

- **Context**: 用户盘中问「南网储能」，Agent 用 MCP `analyze_and_suggest` 报 16.34 当现价；东财实时约 15.47～15.50，口径错误。
- **Pitfall/Error**: `analyze_and_suggest` / `get_stock_daily_data` 取 Tushare `pro.daily` **最后一根已收盘 K 线**（常为前一交易日或当日未收盘前的上一根）；`持仓执行卡.md` 现价若未从 **东方财富证券** 同步也会过期。勿把「最新收盘价」写成「现价」。
- **Solution/Rule**:
  - **盘中现价 / 涨跌幅 / 操作建议**：`uv run python scripts/tools/fetch_eastmoney_quotes.py <code>` 或 `fetch_sop_snapshots` / `intraday_trade_signal`（OpenCLI 东财）。
  - **历史日线 / 回测 / 均线（收盘口径）**：MySQL `stock_daily` 或 Tushare MCP；报告中须标注「最近交易日收盘（非实时）」。
  - **输出模板**：并列三列——**昨收**、**今价（OpenCLI）**、**执行卡触发价**；持仓盈亏用今价重算。
  - **MCP 文案**：`tushare_mcp.py` 的 `analyze_and_suggest` 已改为「最近交易日收盘」；改后需重启 `user-tushare-stock-advisor` MCP。

### 2026-06-02 投顾模式选股过滤（advisor_selection）

- **Context**: 6 万账户投顾阶段 0；用户要求选股与投顾一致，非仅执行卡复述。
- **Pitfall/Error**: 阶段 0 `selection_watchlist --sync` 会推送「像要买」的监控，与 A 档降仓矛盾；但跳过 Top5 SOP 会拖累公众号 `top5` 稿质量。
- **Solution/Rule**: `stock_ai/advisor_selection.py` 读 `投顾主策略.md` 阶段；阶段 0：`combined` 买入类→「继续观察」、Top5 仅情报、**Top5 SOP 默认关**（`DISABLE_SOP_TOP5=1`）、跳过 AI 简评、`--sync` 跳过 alert_rules。进阶段 1 改主策略阶段标记；需 SOP 时设 `DISABLE_SOP_TOP5=0`。

### 2026-06-02 财经快讯利好/利空标记（每 15 分钟）

- **Context**: 用户要求东财 7×24 快讯（`sync_macro_news` / launchd 每 15min）在看板标注利好或利空。
- **Pitfall/Error**: ① 未跑 `stock-mysql/sql/011_macro_news_sentiment.sql` 时 `upsert_news_items` 写 `sentiment` 会失败。② 关键词分类对地缘/宏观叙述常标「中性」，勿当成 LLM 个股研判。③ 勿在 15min 同步里默认再调 Cursor 逐条判读（慢、耗 token）。
- **Solution/Rule**:
  - **分类**：`scripts/tools/news_sentiment.py` → `classify_news_sentiment`（`bullish`/`bearish`/`neutral`；展示 利好/利空/中性）。
  - **落库**：`macro_news_items.sentiment`；`news_db.upsert_news_items` 写入；`refresh_sentiment_for_recent(hours=72)` 在每次 `sync_macro_news` 后补刷。
  - **调度**：`sync_macro_news.sh` + `com.user.stock-macro-news-sync`（900s）；AI 解读仍 `news_ai_interpret.save_news_ai_snapshot`，快讯行前缀 `[利好]` 等。
  - **看板**：home-hub `/news` — `NewsItem.sentiment`、`meta.sentiment_counts`；公开 AI 解读无持仓段（`news_ai_interpret.sanitize_public_ai_summary` + 生成 prompt 禁止 P0～P4）。
  - **迁移**：`cd stock-mysql && bash scripts/init-db.sh`（含 011）。

### 2026-06-03 微信公众号成稿（evening 三篇 + wechat_mp_*）

- **Context**: `stock-ai/scripts/tools/wechat_mp_*.py` 推日更草稿；**晚间金标准**见 skill `evening-trilogy-templates.md`；调度见 `project-memory.mdc` §2026-06-03 微信公众号。
- **Pitfall/Error**: ① `render_article_content_html` / `sync_article_content_from_body` 若 `upload_figures=False` 且无 banner 缓存 → 丢顶栏。② 正文 LLM 免责段 + `disclaimer_html` → 双免责。③ CPS 勿再用 `_CPS_AFTER_SECTION` 分节定位（已删，统一 2/3）。
- **Solution/Rule**:
  - **入口**：`wechat_mp_draft` / `wechat_mp_draft_batch`；`build_article` → `wechat_mp_content.py`。
  - **market 后处理**：`finalize_market_body`（开篇 + 结构三分段 + 拆长段）→ `inject_market_figures`。
  - **HTML**：`text_to_html(article_kind=)`；`opening_lede_paragraph_style`；`attach_footer_product` → `inject_cpsad_at_body_ratio`。
  - **SEO/话题**：`wechat_mp_seo.py`；`append_hashtag_inline_to_body`；`WECHAT_MP_READ_SOURCE_URL` 默认 0。
  - **测试**：`pytest tests/unit/test_wechat_mp_*.py -q`（含 `test_cps_at_two_thirds_*`、`test_*_opening_lede_*`）。
  - **Skill**：`.cursor/skills/wechat-mp-drafts/`（带货用 `wechat-mp-commerce-drafts`）。

### 2026-06-04 公众号 evening 封面（top5 / dragons）

- **Context**: 用户要求选股、龙头草稿封面改为亮色 A 股相关图，定稿后留档。
- **Pitfall/Error**: 仅改素材库名仍会命中旧暗色图（如 K线暗色）；OpenCLI 行业榜误抓「资金流」等 Tab 词。
- **Solution/Rule**: `wechat_mp_client._DEFAULT_KIND_THUMB_ASSETS` — **top5**=`cover-financial-screen-dual.jpg`，**dragons**=`cover-multi-screen-dual.jpg`；`WECHAT_MP_KIND_THUMB_FROM_ASSETS=1` 优先上传本地图。换图删 `data/wechat_mp_thumb_{top5,dragons}.json` 再 `wechat_mp_draft --kind …`。真源 `stock-ai/assets/wechat_mp/COVER_THUMBS.md`。

### 2026-06-04 晚间完读钩子 + 移动排版（wechat_mp_*）

- **Context**: 晚间 `sector`/`top5`/`dragons` 完读与手机阅读体验代码化；用户定稿后更新金模版。运营规则见 `project-memory.mdc` §2026-06-04 晚间三篇金模版。
- **Pitfall/Error**: Top5 标题 echo 进正文（`wechat_mp_monetization` / LLM）；`inject_closing_suspense` 重复行需 `split(marker)` 插入；勿在 polish 里写「往下看」「→」。
- **Solution/Rule**:
  - **钩子**：`wechat_mp_read_hooks.py` → `wechat_mp_sector_polish.finalize_sector_body`、`wechat_mp_top5_polish.finalize_top5_body`、`wechat_mp_dragons_polish.finalize_dragons_body`；`build_top5_article` 末步 `strip_top5_title_echo`。
  - **排版**：`wechat_mp_readability`（`polish_mobile_readability`、`reflow_inline_numbered_lists`）← `wechat_mp_public.finalize_public_body_text`；env `WECHAT_MP_PARA_MAX_CHARS`、`WECHAT_MP_READ_HOOKS`。
  - **变现 prompt**：`wechat_mp_monetization` 要求勿写「往下看」；钩子由 polish 补。
  - **测试**：`test_wechat_mp_read_hooks.py`、`test_wechat_mp_top5_polish.py`、`test_wechat_mp_readability.py`（若有）。
  - **金模版**：`.cursor/skills/wechat-mp-drafts/evening-trilogy-templates.md`（2026-06-04）。

### 2026-06-05 公众号 top5 · 东财人气榜（流量优先）

- **Context**: 搜一搜搜索词为个股名；top5 默认改东财 A 股人气榜选题。
- **Pitfall/Error**: 多策略总分与热搜票错位；纯热股须滤 ST/禁码；`emappdata` 裸 HTTP 常异常，须 OpenCLI 打开 vipmoney 人气页解析 DOM。
- **Solution/Rule**: `WECHAT_MP_TOP5_POOL=hot`（默认）· `WECHAT_MP_TOP5_HOT_N=10` · `fetch_hot_stocks_opencli` · `wechat_mp_hot_stocks.load_wechat_top5_hot_picks`；失败回退 `load_wechat_top5_selection_picks`。测试 `test_wechat_mp_hot_stocks.py`。

### 2026-06-03 定时去重（scheduler vs launchd）

- **Context**: 持仓监控与文档/安装脚本对齐。
- **Pitfall/Error**: `holdings-monitor` launchd 与 Docker `*/5 monitor` 重复；战报 09/12/15/20 文档过时。
- **Solution/Rule**: 仅 Docker 触发监控；快讯用 `com.user.stock-macro-news-sync`；重装 `./scripts/install-stock-ai-scheduler.sh` 会删除旧 plist。见 `project-memory.mdc` §2026-06-03 stock-ai 定时任务去重。

### 2026-06-04 综合选股前日线门禁（ensure_daily_bars）

- **Context**: 17:30 四轨选股依赖 MySQL `stock_daily`；若 17:00 容器 sync 未完成或条数不足，combined 会用旧 K 线扫全市场。
- **Pitfall/Error**: 仅看 `MAX(trade_date)` 不够（可能日期到了但当日行数极少）；`ensure_daily_bars` 以前补同步失败仍 exit 0，流水线继续跑。
- **Solution/Rule**:
  - **模块**：`scripts/tools/ensure_daily_bars.py` — `check_daily_bars_ready()`（期望交易日 + 当日 `COUNT(*)≥3500`，阈值 `DAILY_BARS_MIN_COUNT`）；`ensure_daily_bars_at_selection_start()`（落后则 `by_date` 补同步，仍不满足 **exit 1**）。
  - **期望日**：工作日 17:00 前 = 上一交易日，17:00 后 = 当天（`tushare_ready_hour=17`）。
  - **入口**：`run_selection_daily.sh` → `--sync-if-stale --require-ready`；`run_parallel_selection.main()` 与 `stock_selection_combined.main()`（无 `target_date` 时）开头各调一次；跳过 `SKIP_DAILY_BARS_CHECK=1`。
  - **CLI**：`uv run python -m scripts.tools.ensure_daily_bars`（只读状态）；`--sync-if-stale --require-ready`（与自动化一致）。
  - **调度**：`docs/SCHEDULING.md` — 工作日 **17:30** sync、`17:45` 选股、`18:00` 龙头 eod（`--days 2` 两交易日）；容器 `sync_emotion_cycle.sh` 用 `/usr/local/bin/uv`；改 crontab 后 **rebuild** scheduler。

### 2026-06-05 dragons 标题 echo 进正文

- **Context**: 龙头 evening 稿标题「情绪发酵怎么玩？大有能源5板还在榜」被 LLM 贴在「> 情绪与盘面」之前。
- **Pitfall/Error**: top5 已有 `strip_top5_title_echo`，dragons 未做对称处理；变现 prompt 只写标题模板未禁正文复述。
- **Solution/Rule**: `strip_dragon_title_echo`（`wechat_mp_dragons_article.py`）— 精确 title 匹配 + `怎么玩…还在榜` 正则；`build_dragons_article` 与 LLM 返回后各调一次；prompt/变现块补禁句。测试 `test_strip_dragon_title_echo_removes_title_line`；skill `rules-implemented.md` · `evening-trilogy-templates.md`。

### 2026-06-12 公众号 · 平台推荐安全（财产风险 / 不适合推荐）

- **Context**: 微信后台对财经 evening 稿标注「财产安全 / 不适合推荐」；需在推稿前把标题、摘要、正文用语从「操作指引 / 推荐 / 理财」口吻改为「信息整理 / 结构观察」。
- **Pitfall/Error**: ① `check_public_compliance` 原先未覆盖标题式触发词（`怎么玩` `还在榜` `领衔`）。② LLM/增长模板仍产出旧标题池。③ `sanitize_platform_property_risk` 勿替换正文固定小节名（如 `> 明日计划与纪律`）。④ `split_disclaimer` 仍识别旧 mark「投资日记」以便拆旧稿。
- **Solution/Rule**:
  - **模块**：`wechat_mp_public.py` — `PLATFORM_PROPERTY_RISK_RULE`（LLM prompt）、`PLATFORM_PROPERTY_RISK_CHECKS`（扫描）、`_TITLE_RISK_INLINE_REPLACEMENTS` + `sanitize_public_title(kind=…)`（标题终稿；dragons 可把「怎么玩…还在榜」重建为 `{phase}梯队｜{lead}{N}板结构`）、`audit_recommendation_safety`（标题+摘要+正文合并审计）、`INFORMATION_NOTICE`（finance kind 正文 prepend）。
  - **免责常量**：`wechat_mp_content.DISCLAIMER` = 「市场信息整理与复盘笔记」；`wechat_mp_product` / `wechat_mp_stock_ai_cta` / `wechat_mp_monetization` 同步改 mark。
  - **推稿**：`wechat_mp_draft_batch.run_batch` 用 `audit_recommendation_safety` 替代仅扫 `body_text`；`_article_shell` 写 title 前 `sanitize_public_title`。
  - **SEO**：`wechat_mp_seo.title_sousou_hook_score` 对 `怎么玩/还在榜/领衔/明日盯` 负分；`enrich_title_for_search` 末步 `sanitize_public_title`。
  - **勿回退**：正文小节 **`> 明日计划与纪律`** 保留原名；仅标题禁「明日盯/怎么玩」类问句。
  - **测试**：`test_wechat_mp_public.py::test_sanitize_public_title_strips_old_winners`（含 6/12 四标题对照）；`pytest tests/unit/test_wechat_mp_public.py tests/unit/test_wechat_mp_seo.py tests/unit/test_wechat_mp_evening_align.py -q`。
  - **运营索引**：`project-memory.mdc` §2026-06-12。

### 2026-06-14 公众号 · 【说明】开篇重复叠加

- **Context**: 6/14 周日 news 草稿箱预览，文首叠 **3 段相同【说明】**；用户反馈与平台审核优化同一轮处理。
- **Pitfall/Error**: ① `_article_shell` / `attach_publish_hints` → `sync_article_content_from_body` 会 **多次** 调 `render_article_content_html`。② `sanitize_platform_property_risk` 把说明里的「投资操作」「收益判断」改成「市场观察」「量价表现」，导致 `notice not in core` 失效，每次 sync **再 prepend 一段**。③ 旧稿须手动重推或等下一批；代码 fix 不 retroactive 已上传 HTML。
- **Solution/Rule**:
  - **模块**：`wechat_mp_public.py` — `strip_information_notices()` + `_INFORMATION_NOTICE_LINE_RE`；`sanitize_platform_property_risk` **跳过**说明行；`_core_body_for_audit` 先 strip 说明。
  - **渲染**：`wechat_mp_content.render_article_content_html` — finalize **前** strip 全部旧说明，finalize **后** 只 prepend **一次** `INFORMATION_NOTICE`（去掉 `notice not in core` 判断）。
  - **测试**：`test_wechat_mp_disclaimer_render.py::test_information_notice_not_duplicated_on_re_render`。
  - **已污染草稿修复**：从 draft HTML 抽正文 → `strip_information_notices` → `render_article_content_html` → `upsert_draft_article`。
  - **运营索引**：`project-memory.mdc` §2026-06-14。

### 2026-06-16 公众号 · 内容分析 OpenCLI（页面柱图 · 逐日渠道）

- **Context**: `fetch_wechat_mp_analytics_opencli.py` 复盘流量来源；用户确认 **页面 highcharts 为准**，弃 API 交叉校验。
- **Pitfall/Error**: ① `_fetch_source_api` / `begin=end` 与后台柱图 **不一致**（勿再默认调用）。② `SET_FLOW_DATE_RANGE_JS` 旧版点箭头导航失败（picker 头栏仅左键）；改 **点「MM月」→ 选月 → 点非 faded 的 `<a>` 日**。③ `CLICK_PERIOD_TAG_JS` 须 `f"({JS})({json.dumps(label)})"`，勿 `.replace("periodText", …)` 破坏参数名。④ 柱图 `%` 与 X 轴标签须 **按 cx 对齐**（`EXTRACT_HIGHCHARTS_TRAFFIC_JS`），勿固定渠道顺序。
- **Solution/Rule**:
  - **模块**：`wechat_mp_analytics_page.py` — `SET_FLOW_DATE_RANGE_JS` · `EXTRACT_HIGHCHARTS_TRAFFIC_JS`；`fetch_wechat_mp_analytics_opencli.py` — `fetch_daily_traffic_series_page()` · CLI `--daily-series --series-begin --series-end`；默认 `traffic_sources_pct` **来自 highcharts**。
  - **单日抓取**：`--begin-date YYYY-MM-DD --end-date YYYY-MM-DD`（须相等才得单日柱图）。
  - **逐日序列**：`--daily-series --series-begin 2026-06-02 --series-end 2026-06-16 -o output/wechat_mp_recommend_daily_page.json`；JSON 字段 `days[].traffic_sources_pct` · `days[].推荐` · `read_users_total`。
  - **解读**：概况 tag 与流量 picker **独立**；复盘限推看 **picker=单日** 的推荐% 趋势，非默认 30 天柱图。
  - **运营索引**：`project-memory.mdc` §2026-06-16 · `content-analytics-sop.md`。

### 2026-07-31 公众号影视稿 tv_review（大白话 + 排版）

- **Context**: 《蜘蛛侠：崭新之日》试跑定稿；用户反馈「戏眼」「意象」等影评腔不像人话，且 `polish_mobile_readability` 把 tv 稿拆成一句一段。
- **Pitfall/Error**: ① 剧评腔/论文腔（戏眼、正向反馈、遮羞布、场面堆叠、拍实、耳目一新）。② `finalize_public_body_text` 对全稿 `split_long_paragraphs` 默认 120 字按句拆段。③ 首段正文写评分与竖排 bullet 重复。④ 剧照锚点「战衣」误匹配「缝战衣」插到开头。
- **Solution/Rule**:
  - **口吻**：`TV_DEPTH_RULE` + `anti-ai-voice.md` 影视大白话表；禁第一人称、禁百科式「剧情承接/梗概也熟」。
  - **开头**：首段意象/票房 → 脚本注入评分 → 第二段观感；首段不写 7.8。
  - **排版**：`finalize_tv_review_body` + `merge_tv_short_paragraphs`；`polish_mobile_readability(..., kind=tv_review)` **跳过** 120 字拆句。
  - **配图**：`after_anchor` 锚点须与正文逐字匹配（`制服还在`、`台灯底下`）；末段可插第四张图；脚注 TMDB 片用「TMDB 宣传剧照」。
  - **改稿**：`golden` → `save_tv_body_cache` → `wechat_mp_repush_tv_review_draft`。
  - **Skill**：`tv-review-template.md` §院线单片剧评固化规则 · `wechat-mp-writing/tv-review-voice.md`（IGN 仿写）。
