---
description: 个人工具 monorepo — stock-ai、sidestore-infra、substore-clash
alwaysApply: true
---

# tools-workspace

根目录：`/Users/yolo/dev/yolo/tools-workspace`

| 子项目 | 路径 | 用途 |
|--------|------|------|
| stock-ai | `stock-ai/` | A 股数据、Tushare MCP、investment-agent |
| sidestore-infra | `sidestore-infra/` | SideStore、Caddy、阿里云 DDNS、证书 |
| substore-clash | `substore-clash/` | Sub-Store 订阅合并 → Mihomo/Clash 配置 |

## 注意

- 项目已物理迁入本工作空间；`~/sidestore-infra` 与 `~/dev/yolo/stock-ai` 仅为兼容符号链接
- launchd、Docker 使用 `sidestore-infra/` 下的真实路径
- **单一 git 仓库**在 `tools-workspace` 根目录；勿在子目录保留嵌套 `.git`

## Agent 工作流

- **Superpowers**：`.cursor/rules/superpowers.mdc` — 设计、TDD、计划、调试（`/add-plugin superpowers` 启用插件）
- **Hermes**：`.cursor/rules/hermes-protocol.mdc` — 检索 `project-memory.mdc` / `memory-*.mdc`，结束后询问是否写入记忆

详见根目录 `AGENTS.md`。
