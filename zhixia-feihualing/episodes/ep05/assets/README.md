# EP05素材投放区

本集素材统一放在这里，不再要求用户去全局视频、音频目录中寻找。

- `video/`：放Seedance下载的15秒主内容，推荐命名 `content-raw-v01.mp4`。
- `audio/`：如手动下载配音，按 `01-opening.mp3` 至 `05-outro.mp3` 命名；自动TTS仍会先输出到全局兼容目录，后期确认采用版本后复制到这里。
- `images/`：放本集封面底图、字幕预览或其他本集专用图片。

不要覆盖旧版本。重做素材依次使用 `v02`、`v03`。
