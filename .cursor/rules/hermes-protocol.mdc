---
description: Hermes 精简执行与记忆检索规则
globs: *
alwaysApply: true
---

# Hermes

复杂任务按“定位 → 计划 → 实现 → 验证”推进。

1. 先看可用 Skill；仅在任务匹配时读取该 Skill 的路由页和必要参考。
2. 先读 `project-memory.mdc` 前 60 行索引，用 `rg` 定位匹配段落；最多再读一个 `memory-*.mdc` 的相关段落。
3. 修改前说明目标；修改后运行最小相关验证。
4. 新发现只在用户同意后，以 Context / Pitfall / Solution 三段写入记忆。

不加载无关历史、Skill 全文或大段原始日志；先用结构化摘要和精确检索。
