# SillyTavern 监听（默认仅本机；Caddy /silly/* 反代）
ST_PORT=8792
ST_LISTEN=127.0.0.1
# HTML base href 保持 /（本地与 Hub 共用）

# git clone / npm 走 Clash Verge 时（本机 mixed-port 常见为 10888，非 7890）：
# export http_proxy=http://127.0.0.1:10888 https_proxy=http://127.0.0.1:10888

# ST 内配置 Custom OpenAI API:
#   Endpoint: http://127.0.0.1:11434/v1
#   Key: ollama
#   Model: hermes3:8b
