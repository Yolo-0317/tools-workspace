---
description: 工作空间全局禁止在用户可见界面与文案中使用 emoji
alwaysApply: true
---

# 禁止使用 Emoji

本工作空间 **所有子项目**（`stock-ai/`、`harryputter/`、`english-buddy/`、`home-hub/` 等）统一遵守：

## 范围

- **用户可见 UI**：按钮标签、导航、提示、占位符、锁屏/通知文案、PWA 界面
- **对外内容**：公众号草稿、文档面向读者的正文、错误/成功提示
- **Agent 产出**：向用户展示的说明、清单、状态汇报（优先用文字或符号如 `×` `·` `—`）

## 允许

- 代码注释、日志、第三方 API 返回的原始数据（勿改上游）
- 用户明确要求使用 emoji 的例外（须在其消息中写明）

## 替代

| 避免 | 改用 |
|------|------|
| ⏱ 定时 | 文字「定时」或 SVG 图标 |
| 🔊 发音 | 「发音」「播放」或图标字体 |
| ✓ / 勾选装饰 | 「完成」「OK」或 CSS |

## Agent 执行

- 新增或修改 UI/文案时 **不得** 引入 emoji
- 发现既有 emoji 应改为文字或内联 SVG，与周围控件风格一致
